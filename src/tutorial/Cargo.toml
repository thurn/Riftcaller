[package]
name = "tutorial"
version = "0.0.0"
homepage = "https://github.com/thurn/spelldawn"
description = "Tutorial for how to play the game"
license = "Apache-2.0"
edition = "2021"

[lib]
path = "src/tutorial.rs"
test = false
doctest = false


[dependencies]
actions = { path = "../actions", version = "0.0.0" }
data = { path = "../data", version = "0.0.0" }
rules = { path = "../rules", version = "0.0.0" }
with_error = { path = "../with_error", version = "0.0.0" }

anyhow = "1.0.58"
once_cell = "1.13.0"