[package]
name = "card_definition_data"
version = "0.0.0"
description = "Datatypes for defining cards"
license = "Apache-2.0"
edition = "2021"

[lib]
doctest = false

[dependencies]
with_error = { path = "../../with_error", version = "0.0.0" }
adventure_data = { path = "../adventure_data", version = "0.0.0" }
game_data = { path = "../game_data", version = "0.0.0" }
core_data = { path = "../core_data", version = "0.0.0" }

anyhow = "1.0.58"
enum-kinds = "0.5.1"